"""
Константы проекта.
"""

DEFAULT_MOCK_RESPONSE = "## Requirement 1 (maintainability)\n\n{\n  \"score\": 5,\n  \"max_score\": 10,\n  \"justification\": \"В репозитории отсутствует явный «one‑command» скрипт/файл сборки (нет Makefile, setup.py, pyproject.toml, package.json или аналогичного). (Файлы в корне: VendingMachine.py, test_vending_machine.py, bugs.txt, .coverage). Тем не менее тестовый набор реализован с использованием стандартного unittest (в test_vending_machine.py есть защитный блок if __name__ == '__main__': unittest.main()), поэтому тесты можно запустить одной стандартной командой Python: e.g. python -m unittest test_vending_machine.py или python -m unittest discover -v — и они выполнятся без сторонних зависимостей. Из этого следует: функционально проект можно проверить одной командой, но репозиторий не содержит явного/документированного «make test»/«mvn test»/«npm test» целевого файла.\",\n  \"suggestions\": \"Добавьте явную команду/мишень для запуска сборки/тестов и задокументируйте её в README, например: • Makefile с мишенью test: python -m unittest discover -v • или скрипт run_tests.sh • либо pyproject.toml / tox.ini / GitHub Actions workflow для автоматического запуска. Это сделает «одну команду» очевидной и воспроизводимой для пользователей/CI.\"\n}\n\n## Requirement 2 (maintainability)\n\n{\n  \"score\": 0,\n  \"max_score\": 10,\n  \"justification\": \"В корне репозитория отсутствует файл README (ни README, ни README.md, ни README.rst). Структура проекта в описании: \\\".\\\", \\\".coverage\\\", \\\"VendingMachine.py\\\", \\\"bugs.txt\\\", \\\"test_vending_machine.py\\\" — README не перечислен. Единственные возможные подсказки по использованию находятся внутри теста (test_vending_machine.py), где есть комментарии, но это не заменяет полноценный README с инструкцией по запуску и примером использования.\",\n  \"suggestions\": \"Добавьте файл README.md в корень репозитория. Он должен содержать минимум:\\n- Краткое описание проекта (что делает VendingMachine).\\n- Требования/версия Python.\\n- Инструкцию по запуску тестов (например: \\\"python -m unittest\\\" или \\\"pytest\\\").\\n- Пример использования класса VendingMachine (короткий код: import VendingMachine; vm = VendingMachine(); vm.put_coin1(); ... и ожидаемые ответы).\\n- При необходимости описание возвращаемых кодов Response и краткая инструкция по переходу в админ-режим.\\nПример минимального README.md можно добавить сразу и сопроводить командой проверки: \\\"python -m unittest test_vending_machine.py\\\".\"\n}\n\n## Requirement 3 (functional)\n\n{\n  \"score\": 7,\n  \"max_score\": 10,\n  \"justification\": \"В большинстве публичных методов реализована проверка входных значений и возвращаются коды ошибок вместо возбуждения исключений: VendingMachine.fill_coins (проверки c1/c2 -> INVALID_PARAM), enter_admin_mode (проверка баланса и кода -> CANNOT_PERFORM / INVALID_PARAM), set_prices (p <= 0 -> INVALID_PARAM), put_coin1/put_coin2 (проверка режима и переполнения -> ILLEGAL_OPERATION / CANNOT_PERFORM), return_money и give_product (проверки баланса/остатка/количества товара -> соответствующие Response). См. VendingMachine.py: функции fill_coins, enter_admin_mode, set_prices, put_coin1, put_coin2, return_money, give_product — они возвращают VendingMachine.Response.* вместо raise. Однако отсутствует проверка типов входных парамет (например c1, c2, p, number) и нет перехвата TypeError/ValueError: сравнения вида c1 <= 0, c2 % self._coinval2 и арифметика с параметрами могут привести к TypeError при передаче None/строки и тогда приложение упадёт. Нет единого механизма \\\"fail-safe\\\" уровня API для перехвата непредвиденных исключений.\",\n  \"suggestions\": \"1) Добавить явную проверку типов для входных параметров в публичных методах (isinstance(..., int)) и возвращать VendingMachine.Response.INVALID_PARAM при несоответствии. 2) Обернуть критичные места (сравнения/арифметика с внешними параметрами) в try/except TypeError/ValueError и конвертировать ошибки в читабельные коды (INVALID_PARAM или CANNOT_PERFORM). 3) Рассмотреть добавление глобального перехвата непредвиденных исключений на уровне API (логирование + возврат безопасного кода), чтобы гарантировать отсутствие аварийного завершения при некорректном использовании API. 4) Добавить юнит-тесты на некорректные типы/значения (None, строки, float), чтобы зафиксировать ожидаемое поведение.\",\n  \"evidence\": [\n    \"VendingMachine.py: fill_coins — проверка параметров и возврат INVALID_PARAM (lines: def fill_coins(self, c1: int, c2: int): if c1 <= 0 or c1 > self._maxc1: return VendingMachine.Response.INVALID_PARAM ...)\",\n    \"VendingMachine.py: enter_admin_mode — проверка баланса и кода, возврат CANNOT_PERFORM/INVALID_PARAM (def enter_admin_mode(self, code: int): if self._balance != 0: return VendingMachine.Response.CANNOT_PERFORM; if code != self._id: return VendingMachine.Response.INVALID_PARAM ...)\",\n    \"VendingMachine.py: set_prices — проверка p <= 0 -> INVALID_PARAM (def set_prices(self, p: int): if self._mode == VendingMachine.Mode.OPERATION: return VendingMachine.Response.ILLEGAL_OPERATION; if p <= 0: return VendingMachine.Response.INVALID_PARAM ...)\",\n    \"VendingMachine.py: put_coin1 / put_coin2 — проверки режима и переполнения, возвращают ILLEGAL_OPERATION / CANNOT_PERFORM, изменяют _balance/_coins* (def put_coin1(self): if self._mode == VendingMachine.Mode.ADMINISTERING: return VendingMachine.Response.ILLEGAL_OPERATION; if self._coins1 == self._maxc1: return VendingMachine.Response.CANNOT_PERFORM ...)\",\n    \"VendingMachine.py: return_money / give_product — комплексные проверки и возврат соответствующих Response вместо исключений (см. тело функций return_money и give_product).\",\n    \"Missing: нет проверок типов входных парамет и нет try/except вокруг операций, которые могут бросить TypeError при некорректных типах (например c1 <= 0 при c1 == None).\"\n  ],\n  \"conclusion\": \"Требование выполнено частично: большое количество валидаций и обработок ошибок реализовано через возвращаемые коды (что предотвращает аварийное завершение в нормальных сценариях). Однако отсутствие проверки типов входных данных и отсутствующий перехват неожиданных исключений оставляют риск аварийного завершения при некорректном (нечисловом) вводе — требуются доработки.\"\n}\n"