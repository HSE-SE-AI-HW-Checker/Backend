# LLaMA Local

Локальный запуск квантизированных моделей LLaMA на MacOS с поддержкой Metal для Apple Silicon.

## Возможности

- 🚀 Запуск квантизированных моделей LLaMA (Q4_K_M) локально
- 📥 Автоматическая загрузка моделей с Hugging Face
- 🎯 Поддержка Metal для ускорения на Apple Silicon
- 📝 Streaming вывод токенов в реальном времени
- 🌐 FastAPI интеграция с REST API
- ⚙️ Гибкая конфигурация через YAML
- 🛡️ Graceful shutdown по Ctrl+C
- 📊 Структурированное логирование

## Требования

- macOS (с Apple Silicon для Metal поддержки)
- Python 3.10 или выше
- Квантизированная модель LLaMA в формате GGUF

## Установка

### 1. Клонирование репозитория

```bash
cd llama_local
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
source venv/bin/activate
```

### 3. Установка зависимостей

Для установки llama-cpp-python с поддержкой Metal:

```bash
CMAKE_ARGS="-DLLAMA_METAL=on" pip install llama-cpp-python
pip install -r requirements.txt
```

Или просто:

```bash
CMAKE_ARGS="-DLLAMA_METAL=on" pip install -r requirements.txt
```

### 4. Настройка конфигурации

Отредактируйте `config.yaml` для настройки автоматической загрузки модели:

```yaml
download:
  # ID репозитория на Hugging Face
  repo_id: "bartowski/Llama-3.2-3B-Instruct-GGUF"
  
  # Имя файла модели для загрузки
  filename: "Llama-3.2-3B-Instruct-Q4_K_M.gguf"
  
  # Автоматическая загрузка модели, если она отсутствует
  auto_download: true

model:
  # Путь к файлу модели (должен совпадать с filename из секции download)
  path: "./models/Llama-3.2-3B-Instruct-Q4_K_M.gguf"
  # ... остальные параметры
```

**Примечание:** При первом запуске модель будет автоматически загружена с Hugging Face, если она отсутствует в директории `./models/`. Это может занять некоторое время в зависимости от размера модели и скорости интернета.

### 5. Ручная загрузка модели (опционально)

Если вы предпочитаете загрузить модель вручную, установите `auto_download: false` в `config.yaml` и скачайте модель самостоятельно:

```bash
mkdir -p models
# Скопируйте вашу модель в models/
```

Рекомендуемые модели:
- **LLaMA 3.2 3B Instruct Q4_K_M** (~2.0 GB) - рекомендуется, хороший баланс качества и производительности
- **LLaMA 3.2 1B Instruct Q4_K_M** (~0.7 GB) - для слабых систем
- **LLaMA 3.1 8B Instruct Q4_K_M** (~4.9 GB) - для мощных систем с большим объемом RAM

Источники моделей:
- [Hugging Face](https://huggingface.co/models?search=llama-3.2-gguf)
- [bartowski's GGUF models](https://huggingface.co/bartowski)

## Использование

### Запуск приложения

```bash
python main.py
```

Или с указанием пути к конфигурации:

```bash
python main.py path/to/config.yaml
```

### Интерактивная работа

1. После запуска введите ваш промпт
2. Нажмите **Ctrl+D** для отправки промпта модели
3. Модель начнет генерировать ответ в режиме streaming
4. Для выхода нажмите **Ctrl+C**

### Пример сессии

```
======================================================================
LLaMA Local - локальный запуск квантизированных моделей LLaMA
======================================================================

Инструкции:
  - Введите промпт и нажмите Ctrl+D для отправки
  - Нажмите Ctrl+C для выхода из программы
======================================================================

Введите промпт (Ctrl+D для завершения ввода, Ctrl+C для выхода):
> Что такое машинное обучение?
[Ctrl+D]

Ответ модели:
----------------------------------------------------------------------
Машинное обучение - это раздел искусственного интеллекта...
----------------------------------------------------------------------
```

## API Режим

LLaMA Local поддерживает работу в режиме REST API сервера, что позволяет интегрировать модель в веб-приложения, мобильные приложения и другие сервисы.

### Запуск API сервера

```bash
cd llama_local
python api_server.py
```

Сервер будет доступен по адресу: `http://localhost:8000`

### Интерактивная документация

После запуска сервера доступна автоматически сгенерированная документация:

- **Swagger UI**: `http://localhost:8000/docs`
- **ReDoc**: `http://localhost:8000/redoc`

### Доступные эндпоинты

- `POST /generate` - Генерация текста (streaming и non-streaming)
- `GET /health` - Проверка состояния сервера
- `GET /model-info` - Информация о загруженной модели

### Быстрый пример

```bash
# Проверка здоровья сервера
curl http://localhost:8000/health

# Генерация текста
curl -X POST "http://localhost:8000/generate" \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "Что такое машинное обучение?",
    "temperature": 0.7,
    "max_tokens": 512,
    "stream": false
  }'
```

### Подробная документация

Полное руководство по использованию API, включая примеры на Python, JavaScript, интеграцию в приложения и обработку ошибок, доступно в [`API_USAGE.md`](API_USAGE.md).

## Конфигурация

Основные параметры в `config.yaml`:

### Параметры загрузки

- `repo_id` - ID репозитория на Hugging Face (например, "bartowski/Llama-3.2-3B-Instruct-GGUF")
- `filename` - имя файла модели для загрузки (например, "Llama-3.2-3B-Instruct-Q4_K_M.gguf")
- `auto_download` - автоматически загружать модель, если она отсутствует (true/false)
- `token` - токен Hugging Face для приватных моделей (опционально)

### Параметры модели

- `path` - путь к файлу модели GGUF
- `n_ctx` - размер контекста (количество токенов)
- `n_gpu_layers` - количество слоев на GPU (-1 = все слои)
- `temperature` - креативность генерации (0.0-2.0)
- `top_p` - nucleus sampling (0.0-1.0)
- `top_k` - top-K sampling
- `repeat_penalty` - штраф за повторения
- `max_tokens` - максимум токенов в ответе
- `stream` - включить streaming вывод

### Параметры приложения

- `log_level` - уровень логирования (DEBUG, INFO, WARNING, ERROR, CRITICAL)

### Использование токена Hugging Face

Для загрузки приватных моделей или увеличения лимитов скачивания, вы можете указать токен Hugging Face:

1. **В конфигурации:**
   ```yaml
   download:
     token: "your_hf_token_here"
   ```

2. **Через переменную окружения:**
   ```bash
   export HF_TOKEN="your_hf_token_here"
   python main.py
   ```

Получить токен можно на [Hugging Face Settings](https://huggingface.co/settings/tokens).

## Структура проекта

```
llama_local/
├── src/                      # Исходный код модулей
│   ├── __init__.py          # Инициализация пакета
│   ├── logger.py            # Модуль логирования
│   ├── config_manager.py    # Управление конфигурацией
│   ├── signal_handler.py    # Обработка сигналов
│   ├── input_handler.py     # Обработка ввода
│   ├── model_manager.py     # Управление моделью
│   └── model_downloader.py  # Загрузка моделей с Hugging Face
├── models/                   # Директория для моделей (создается автоматически)
├── main.py                   # Главный модуль приложения (CLI режим)
├── api_server.py            # FastAPI сервер (API режим)
├── config.yaml              # Конфигурация
├── requirements.txt         # Зависимости Python
├── pyproject.toml          # Метаданные проекта
├── .gitignore              # Игнорируемые файлы
├── README.md               # Документация
└── API_USAGE.md            # Документация по API
```

## Архитектура

Проект использует модульную архитектуру с разделением ответственности:

- **ModelDownloader** - автоматическая загрузка моделей с Hugging Face
- **ModelManager** - загрузка модели и генерация ответов
- **InputHandler** - чтение промптов из stdin
- **SignalHandler** - graceful shutdown по Ctrl+C
- **ConfigManager** - загрузка и валидация конфигурации
- **Logger** - структурированное логирование

Подробнее см. `ARCHITECTURE.md`.

## Устранение неполадок

### Ошибка при установке llama-cpp-python

Убедитесь, что установлены необходимые инструменты сборки:

```bash
xcode-select --install
```

### Модель не загружается

Проверьте:
- Путь к модели в `config.yaml` корректен
- Если используется автоматическая загрузка:
  - `repo_id` и `filename` указаны правильно
  - Есть подключение к интернету
  - Достаточно свободного места на диске (минимум 5 ГБ)
- Файл модели существует и имеет формат GGUF
- Достаточно свободной памяти

### Ошибка при загрузке модели с Hugging Face

Если возникают проблемы с загрузкой:
- Проверьте подключение к интернету
- Убедитесь, что `repo_id` и `filename` указаны корректно
- Для приватных моделей укажите токен Hugging Face
- Попробуйте загрузить модель вручную и установите `auto_download: false`

### Медленная генерация

Попробуйте:
- Уменьшить `n_ctx` в конфигурации
- Убедиться, что `n_gpu_layers: -1` для использования Metal
- Использовать модель меньшего размера (1B вместо 3B)

## Лицензия

MIT License

## Поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.